<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <!--
        O ambiente de teste NÃO fica aqui: dentro do container, as variáveis do
        docker-compose chegam pelo $_SERVER, que o Laravel lê antes do $_ENV
        populado por este bloco (mesmo com force="true"). A configuração real
        vive em .env.testing e é aplicada por tests/bootstrap.php — ver o
        comentário lá.
    -->
</phpunit>
